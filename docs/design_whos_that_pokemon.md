I have images for every single pokemon (so far) with transparent backgrounds. stored in assets/images/pokemon
I want to randomly pick one, saturate and contrast the image so that it's a completely black image.
The present that image to the user.

Underneath, they can have a text input field? With autocomplete for the entire pokemon collection. The user can click the autocomplete names. 

This adds a bonus badge if they get 10 right, gold if they get 100 right.

## Snappy Gameplay (Option)
Instead of text entry, provide **4 Multiple Choice buttons**:
- 1 correct name, 3 random decoys from the same generation.
- Keeps the game very fast and snappy.
- If using text input: Add a clear "Submit" button to confirm the selection from autocomplete if it doesn't auto-submit.
